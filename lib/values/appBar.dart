import 'package:flutter/material.dart';

class AppBar {
  AppBar._();
/*
  static appBarDefaultm() {
    return AppBar(
      backgroundColor: Colors.transparent,
      iconTheme: IconThemeData(
        color: Colors.white, 
      ),
      elevation: 0.0,
    );
  }
  static const appBarDefault = AppBar(
    backgroundColor: Colors.transparent,
    iconTheme: IconThemeData(
      color: Colors.white, //change your color here
    ),
    elevation: 0.0,
  );*/
}
